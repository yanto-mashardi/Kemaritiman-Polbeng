import { currentPortalUser } from "../../../lib/authorization";
import { ensureDatabase } from "../../../lib/mysql-database";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const user=await currentPortalUser();if(!user.email)return new Response("Unauthorized",{status:401});const {id}=await params;const db=await ensureDatabase();const file=await db.prepare("SELECT file_name AS fileName,mime_type AS mimeType,file_data AS fileData FROM evidence_uploads WHERE id=?").bind(id).first<{fileName:string;mimeType:string;fileData:Uint8Array}>();if(!file)return new Response("Not found",{status:404});return new Response(file.fileData,{headers:{"Content-Type":file.mimeType,"Content-Disposition":`attachment; filename*=UTF-8''${encodeURIComponent(file.fileName)}`}})}
